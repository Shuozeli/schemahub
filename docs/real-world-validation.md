<!-- agent-updated: 2026-07-30T04:23:54Z -->
# Real-World Validation Portfolio

This portfolio hardens SchemaHub by using it as an actual schema control and
serving plane. It complements unit and integration tests: each scenario starts
real release-mode processes, uses public CLI or gRPC interfaces, exercises a
recognizable producer/consumer workflow, and preserves enough evidence to
reproduce any failure.

The interactive site in `apps/schemahub-demo` is the human-facing index. It may
simulate a command sequence for teaching, but a scenario is not complete until
the corresponding sequence has passed against a real SchemaHub server and
compiler.

## Completion Contract

Every scenario must:

1. Declare its business story, actors, repository policy, schema formats, and
   expected compatibility decisions.
2. Create deterministic schema/data fixtures without depending on a prior
   scenario or execution order.
3. Run the production CLI against a release-mode server with distinct human,
   delegated-agent, and service identities where applicable.
4. Capture commands, stable JSON fields, expected failures, commit/revision
   coordinates, artifact digests, and the relevant server events.
5. Verify the artifact with a real producer or consumer, not only by checking
   that bytes were returned.
6. Exercise restart, retry, concurrency, or rollback when those behaviors are
   part of the story.
7. Leave an automated regression test for every product defect found.
8. Record a result in the bug ledger even when no product defect is found.

Secrets, bearer tokens, temporary database paths, and machine-specific
coordinates must not appear in committed evidence.

## Portfolio

| ID | Scenario | Formats and pressure | State | Exit evidence |
|---|---|---|---|---|
| RW-01 | Delegated agent proposes; human approves; consumer pins | Protobuf; policy, Apply retry identity, real encoded data, immutable artifacts, restart | Passing | Automated release-server runner, generated Rust consumer, sidecar, transcript assertions, and byte/digest comparison |
| RW-02 | Commerce order-contract rollout | Protobuf; additive rollout, breaking rejection, generated producer/consumer bindings, stored revision/digest | Passing | Served v1/v2 Rust bindings decode both directions; retry identity matches; breaking wire-type edit cannot become Ready or move `main` |
| RW-03 | Mobile telemetry evolution | FlatBuffers; defaults, deprecation, old/new readers, generated artifacts | Passing | Served readers prove default/unknown-slot behavior; physical removal fails; generated bytes match across restart |
| RW-04 | Human and agent edit concurrently | Protobuf; stale ETags/bases, JJ conflicts, idempotent retries, restart recovery | Passing | Stale overwrite fails; same-declaration conflict is explicit; resolution preserves both sides; actors and receipts survive restart |
| RW-05 | Batch/stream producer-consumer handoff | Protobuf + FlatBuffers; immutable serving, sidecar coordinates, digest verification, rollback | Passing | Sidecar-pinned bindings replay unchanged data after both repositories advance; explicit per-repo rollback retains v2 revisions |
| RW-06 | Payments dependency closure | Protobuf; multi-file ChangeRecord, reverse discovery, closure digests, imported-schema deletion | Passing | Root artifacts include the shared dependency; old/new generated closures interoperate; deleting the live dependency cannot become Ready |
| RW-07 | Private tenant isolation | Protobuf + HTTP BFF; RBAC, project visibility, repository-scoped search, service serving | Passing | Reader/Writer boundaries fail closed; private project lists and search stay isolated; authorized generated consumer runs |

The seven executable scenarios are guided by
`codelab-human-agent-schema-workflow.md`, `codelab-commerce-protobuf.md`,
`codelab-mobile-telemetry-flatbuffers.md`,
`codelab-concurrent-human-agent.md`, and
`codelab-data-pipeline-handoff.md`,
`codelab-payments-dependency-closure.md`, and
`codelab-private-tenant-isolation.md`. Their fixtures and runners live under
`../codelabs/real-world/`.

## Evidence Layout

Scenario automation uses this repository layout:

```text
codelabs/real-world/
  lib/harness.sh
  consumers/
    Cargo.toml
    src/bin/
  rw-01-human-agent/{README.md,fixtures/,run.sh}
  rw-02-commerce/{README.md,fixtures/,run.sh}
  rw-03-mobile-telemetry/{README.md,fixtures/,run.sh}
  rw-04-concurrent-editors/{README.md,fixtures/,run.sh}
  rw-05-data-pipeline/{README.md,fixtures/,run.sh}
  rw-06-dependency-closure/{README.md,fixtures/,run.sh}
  rw-07-tenant-isolation/{README.md,fixtures/,run.sh}
  run-all.sh
```

`run.sh` is an intended interface, not permission to hide behavior in an opaque
script. A scenario README must list the exact Arrange, Act, and Assert phases,
the supported backend, runtime requirements, expected negative cases, and the
artifacts it leaves under an ignored temporary evidence directory.

### Readiness report

The suite runs `scripts/render-ga-readiness-report.sh` only after all seven
scenario runners succeed. The reporter requires exactly RW-01 through RW-07,
valid non-empty capability/server/transcript evidence, passing result summaries,
and no credential-shaped keys or values. It normalizes each `result.json`,
records its SHA-256 digest, and evaluates the authoritative
`codelabs/real-world/findings.json` ledger.

An open release-blocker or high finding fails closed. Candidate CI additionally
requires a clean Git worktree and binds the report to the candidate source SHA
and Actions run URL. It retains a deterministic
`schemahub-ga-readiness.tar.gz`; the tag workflow downloads that artifact into
the checksummed release assembly. A local report from a dirty development tree
is useful reproduction evidence, but its provenance is explicitly `dirty` and
cannot satisfy the candidate gate.

A lower-severity finding can carry `must_fix_before: MAJOR.MINOR.PATCH`.
Prereleases at that version remain possible for validation, while the stable
version and every later release fail before artifact construction until the
finding is closed. `RW-03-001` therefore served as a hard 1.0 compiler-pin gate even
though its user impact is correctly classified Low.

On 2026-07-25, an isolated SchemaHub build using the then-unpublished
coordinated FlatBuffers tree passed all seven scenarios. Its machine report has digest
`sha256:4726f75cd9583780d0fba068e8227c5217849e893d27cc3f015a79e3baa05931`
and correctly records dirty source provenance plus
`release_authorized: false`. That compiler state is now published at
`59756d23993538b722f68675c35129c3cebb7aa1`, its main Actions run
`30481753669` is green, and SchemaHub's current lock pins the same immutable
revision, closing `RW-03-001`. A clean SchemaHub candidate remains separate
release evidence and still requires publication of the current SchemaHub tree
and the protected staging gates. Both live compiler repository variables now
match `Cargo.lock`.

On 2026-07-29, the current locked release workspace and strict
all-target/all-feature Clippy passed against the published pin and the hardened
dependency graph. A fresh seven-scenario run bound servers to the Tailscale
interface and addressed them through
`shuoze25-yuacx.tail8f3b66.ts.net`; all scenarios passed with zero open
findings at every severity. RW-06 compiles two immutable generated bundles
whose root and dependency use distinct Protobuf packages, nested under
separate version modules in one consumer. The normalized report digest is
`sha256:959ad689d8409363360be90874e032343103c7eaa0f874cef1dfb2835d1e8643`
and the deterministic archive digest is
`sha256:52b6750f64ea2785b98900c51f79f4a4ecacbb621d1b97c28f0f78dae7e5760e`.
The same source passed the pinned zero-vulnerability/exact-warning Rust audit,
both frozen pnpm audits, the production web builds, and the policy contracts
that prevent audit or allowlist drift. Because the broader 1.0 worktree is
dirty, the report correctly records `release_authorized: false` and the release
verifier rejects it as candidate evidence.

After also closing the unbounded project/repository catalog finding
`RV-CATALOG-001`, project-membership finding `RV-MEMBER-001`,
repository-ref finding `RV-REF-001`, bounded GUI-catalog finding
`RV-BFF-CATALOG-001`, and bounded dashboard/ChangeRecord aggregate finding
`RV-BFF-AGGREGATE-001`, a fresh-fixture rerun of RW-01 through RW-07 again
passed on the Tailscale-bound release server with zero open findings. The
current normalized report digest is
`sha256:20b4575efdf4a95411d6213e573949da80bbd7dc7da536076c971cf1b12761b4`;
the CI-equivalent deterministic archive digest is
`sha256:93c203bfaff0491f72fe12dd8964729309d33b7d1ad9015eb1763490afc83aba`.
Two independently packaged archives were byte-identical. The same source
passed 699 release-workspace tests, strict all-target/all-feature Clippy, all
28 PostgreSQL integration tests, the GUI build/bundle/CDP resolver gates, the
remote mock continuation smoke, and the live governed
agent/human/Apply/restart browser acceptance. A neutral-Pwright run also
proved that the second dashboard schema remained absent until real CDP input
requested its continuation. The report remains correctly marked dirty and
unauthorized, and the release verifier rejects it, until the same suite runs
from a clean pushed candidate.

The report intentionally leaves `release_authorized` false. Passing scenarios
do not replace exact-digest staging, real-provider JWT rotation/staleness, or
the user's explicit authorization to push a tag. For a stable tag, the release
workflow separately verifies those staging claims through the protected,
exact-coordinate attestation described in
`codelab-stable-release-staging.md`.

## Bug Ledger

`codelabs/real-world/findings.json` is the machine-readable authority used by
the GA gate. This table is its human-readable explanation plus the required
no-defect scenario records.

| Finding | Scenario | Class | Severity | State | Evidence or resolution |
|---|---|---|---|---|---|
| RV-HARNESS-001 | RW-01 site | Validation harness | Low | Fixed | Replaced invalid pnpm native-build placeholders with explicit `esbuild`, `sharp`, and `workerd` approvals; frozen install passes |
| RV-DOCS-001 | RW-01 site | Documentation | Low | Fixed | Corrected pnpm argument forwarding in the Tailscale development command; production browser smoke passes |
| RV-SITE-001 | RW-01 site | Deployment runtime | Medium | Fixed | Next 16 server bundles either retained a runtime `require()` or exceeded the 10 MiB Worker limit; the fully prerenderable demo now ships a static export behind a tiny assets-only Worker, and CI boots it in workerd |
| RV-HARNESS-002 | RW-02 runner | Validation harness | Low | Fixed | Corrected Cargo binary invocations to use the generated underscore target names; clean rerun passes |
| RV-HARNESS-003 | RW-02–RW-05 CI | Validation harness | Low | Fixed | Removed the unnecessary ripgrep runtime dependency after a clean GitHub runner rejected the suite before the first scenario; the portable grep-based assertions pass the full local CI-mode rerun |
| RV-HARNESS-004 | RW-07 runner | Validation harness | Low | Fixed | Matched the structured HTTP 403 assertion to SchemaHub's stable “no role on project” diagnostic; the rerun passes through authorized ads search |
| RV-HARNESS-005 | RW-05 runner | Validation harness | Low | Fixed | Replaced the pipeline consumer's removed `createPipelineEvent` free helper with `PipelineEvent::create` and exported its generated module; RW-05 and the complete portfolio pass against the coordinated compiler |
| RV-HARNESS-006 | Release package contract | Validation harness | Low | Fixed | Replaced `tar \| grep -q` membership assertions that could produce SIGPIPE status 141 under `pipefail` with validation over the complete captured archive listing; 120 consecutive runs and ShellCheck pass |
| RV-HARNESS-007 | RustSec audit contract | Validation harness | Low | Fixed | The fake auditor accepted an invocation shape rejected by a real cargo-audit executable. Absolute overrides now run as `<binary> audit`; both the deterministic scenario matrix and the checksummed real-binary SchemaHub scan pass |
| RV-RELEASE-001 | Stable release workflow | Release publication | High | Fixed | The workflow exposed the stable semantic-version container tag before protected staging completed. It now pushes only a run-unique candidate, assembles and checksums the complete release after binary and staging gates, promotes the accepted digest afterward, refuses a conflicting existing tag, and makes GitHub publication depend on verified promotion |
| RV-RELEASE-002 | Release assembly transfer | Supply-chain integrity | High | Fixed | Final publication previously trusted the downloaded assembly without rejecting unchecksummed extras or replaying `SHA256SUMS`. The workflow now verifies a safe exact file set and every checksum before upload and after download, while retaining the Actions artifact digest |
| RV-RELEASE-003 | Native release reproducibility | Supply-chain integrity | High | Fixed | Direct OpenAPI serialization inherited extension hash-map order and archives inherited filesystem metadata. HTTP and CLI now share recursively key-sorted bytes; packaging normalizes sorted ustar membership, UID/GID, modes, UTC timestamps, and gzip headers; fresh-process and deliberately retimed double-package regressions pass, and every release platform must package twice with `cmp` equality |
| RV-SUPPLY-001 | Locked Rust dependency audit | Supply-chain integrity | High | Fixed | Replaced jsonwebtoken's vulnerable RustCrypto RSA path with AWS-LC, locked `crossbeam-epoch` 0.9.20 and `anyhow` 1.0.104, added an RS256 AWS-LC verification test, and made pinned `cargo-audit` require zero vulnerabilities plus exactly the reviewed warning set through deterministic CI contracts |
| RV-SUPPLY-002 | Locked web dependency audit | Supply-chain integrity | High | Fixed | Locked patched Vite, esbuild, PostCSS, Next.js, and Sharp releases and audits both frozen pnpm graphs in CI. The sole React Router exception covers only unused unstable RSC APIs; the policy contract requires the client-only `BrowserRouter` architecture and rejects any extra exception |
| RV-SUPPLY-003 | RustSec auditor bootstrap | Supply-chain integrity | High | Fixed | cargo-audit 0.22.2's published lock contained vulnerable `crossbeam-epoch` and `quinn-proto` plus unsound `anyhow` and `memmap2`. CI now verifies the exact crates.io source archive, overlays a separately reviewed patched lock, installs that graph with `--locked`, and self-audits it before trusting the SchemaHub scan |
| RV-SUPPLY-004 | Release instrumentation tool | Supply-chain integrity | Medium | Fixed | cargo-auditable's current published lock is clean, but future advisories were outside the tag gate. CI now verifies the exact 0.7.5 source and 48-package lock with the trusted auditor, and every platform forces an isolated installation before invoking the exact binary |
| RV-SUPPLY-005 | Container base provenance | Supply-chain integrity | High | Fixed | The GUI builder, Rust builder, and runtime used mutable base tags, so identical source could produce different images. All three now name exact multi-architecture manifest digests, and CI rejects tag-only stages or unpinned external actions |
| RV-SUPPLY-006 | Container build and acceptance tooling | Supply-chain integrity | High | Fixed | The Dockerfile frontend, overridable pnpm coordinate, moving Node workflow selector, and PostgreSQL/curl CI helper tags remained mutable. The frontend and helper images now use exact multi-architecture manifests, workflow Node is fixed to 24.18.0, pnpm is non-overridable in the image, and CI enforces every coordinate |
| RV-SEC-001 | Bundled GUI filesystem boundary | Local file disclosure | High | Fixed | Runtime validation allowed symlinks beneath `gui_dir`, so a linked asset or favicon could escape the static-file root. Startup now rejects a symlinked `assets` root and every nested symlink; focused release tests cover both shapes |
| RV-HTTP-001 | Bundled GUI cache policy | Cache correctness | Medium | Fixed | Every successful `/assets/*` path received one-year immutable caching. Runtime now recognizes only Vite-style eight-character content-hashed filenames as immutable and gives a successful unhashed asset `no-cache`; positive and negative HTTP tests pass |
| RV-HTTP-002 | Bundled GUI browser security policy | Browser security | High | Fixed | The bearer-token console lacked CSP, framing denial, and privileged-feature restrictions. Successful GUI responses now enforce a self-only, no-inline-script/no-frame/no-form/no-object CSP, deny framing and camera/geolocation/microphone use, and pass router, exact-image, and real Pwright render acceptance |
| RV-CODEGEN-001 | Immutable Protobuf closure codegen | Protobuf Rust code generation | Medium | Fixed | Monolithic output flattened distinct package modules, so fully qualified dependency paths could not compile. SchemaHub now renders the exact closure as a deterministic nested module tree, imports only resolved cross-package roots, requires an explicit multi-file root, and re-exports that requested package; unit contracts, a three-package downstream Cargo build, and RW-06's two-version cross-package consumer pass |
| RV-AUDIT-001 | 1.0 control-plane audit | Audit integrity | High | Fixed | Mutable project/member/repository state had no immutable administrative history. Typed project-partitioned events now commit atomically with resource state across every backend and are exposed through Owner-only paginated gRPC plus CLI JSON |
| RV-AUTHZ-001 | 1.0 concurrent membership administration | Authorization invariant | High | Fixed | Concurrent Owners could both pass the last-Owner check before removing each other. Project-keyed cross-instance coordination now spans authorization, invariant validation, and atomic state/audit commit; deterministic concurrency coverage leaves exactly one Owner |
| RV-AUDIT-002 | 1.0 long-lived control-plane audit | Audit scalability | Medium | Fixed | Each page previously loaded and sorted the complete project history. Resource, event, and immutable order-index entries now commit together; memory, redb, and PostgreSQL expose bounded stable range reads; project-bound cursors and event/index validation fail closed |
| RV-CHANGE-001 | 1.0 long-lived ChangeRecord ledger | Pagination scalability | Medium | Fixed | Each public `ListChanges` page previously decoded, filtered, and sorted the complete global record collection. Repository creation/status indexes now update atomically with record creates/transitions, legacy records backfill once, and existing v1 cursors traverse bounded backend ranges with fail-closed target validation |
| RV-CATALOG-001 | 1.0 long-lived resource catalogs | Pagination scalability | Medium | Fixed | `ListProjects` and `ListRepos` previously loaded and sorted complete resource collections, and repository pages scanned outside their parent project. Atomic active/all project and per-project repository catalogs now provide bounded prefix ranges, one-time backfill, existing v1 tokens, authorization-aware continuation, and fail-closed target validation |
| RV-MEMBER-001 | 1.0 long-lived project membership | Pagination scalability | Medium | Fixed | `ListMembers` previously had no pagination fields and decoded the complete global role collection. Project-bound opaque tokens now traverse bounded identity-ordered ranges over existing project-prefixed role keys, advance through tombstones, fail closed on scoped corruption, and support complete CLI traversal without GUI member enumeration |
| RV-REF-001 | 1.0 long-lived repository ref namespaces | Pagination scalability | Medium | Fixed | `ListBranches` and `ListTags` previously had no pagination fields and materialized every matching ref, while `GetBranch` enumerated all bookmarks. Ref-kind/project/repository/prefix-bound tokens now page stable JJ-view order with at most one page plus lookahead materialized; both CLI commands traverse all pages and Get uses a direct named lookup |
| RV-BFF-CATALOG-001 | 1.0 long-lived GUI catalog navigation | Pagination scalability | Medium | Fixed | The GUI BFF previously returned complete project/repository arrays and ran an N+1 full repository scan for project counts. It now adapts bounded Core pages, binds tokens to kind/project/prefix, omits the expensive count, loads React pages incrementally, and resolves deep links through a bounded exact-prefix lookup |
| RV-BFF-AGGREGATE-001 | 1.0 long-lived GUI repository operation | Pagination scalability | Medium | Fixed | Dashboard schema/branch/tag arrays and the browser ChangeRecord array were unbounded, dashboard summaries could cross a moving bookmark, and each row repeated full-tree reads. One repository/ref-bound composite token pins schema pages to the first immutable commit and advances all dashboard cursors; selected objects and repository-local names batch-load in one tree traversal; exact conflict stats avoid a complete path vector; repository/status-bound ChangeRecord pages consume the durable index; React, HTTP/OpenAPI, and browser continuation contracts pass |
| RW-GUI-001 | SchemaHub operator console | Responsive layout | Medium | Fixed | A live tablet-width run exposed the actor control wrapping over page content. The header now keeps fixed/flexible groups on one line, hides secondary controls at explicit breakpoints, and the browser acceptance asserts geometry at 930 px and 390 px |
| RW-GUI-002 | SchemaHub live operator-console acceptance | Validation harness | Low | Fixed | The live acceptance queried a visual identity label rather than its `Identity:`-prefixed accessible name and retained remote CDP connections after success. Exact agent/human accessible-name assertions and unconditional local/remote browser teardown now let the governed Apply/restart run terminate cleanly |
| RW-03-001 | RW-03 | FlatBuffers Rust code generation | Low | Fixed | Published compiler commit `59756d23993538b722f68675c35129c3cebb7aa1` uses `Type::create`, makes deprecated fields read-only, and fixes struct-union accessors. FlatBuffers main Actions run `30481753669` passed release tests, formatting, and strict Clippy; SchemaHub pins the exact Git revision and its downstream warning contract passes |
| — | RW-01 server | Product | — | No correctness defect found | Real codelab passed the guarded lifecycle, generated data round trip, sidecar, serving, Apply replay, and restart checks |
| — | RW-02 server | Product | — | No correctness defect found | Real additive/breaking lifecycle, Apply replay, artifact verification, and bidirectional generated-reader checks pass |
| — | RW-04 server | Product | — | No correctness defect found | Real stale-ETag, same-base conflict, retry, resolution, artifact, and restart checks pass |
| — | RW-05 server | Product | — | No correctness defect found | Real producer/replay bytes, two repositories, immutable serving, per-repository undo, and retained history checks pass |
| — | RW-06 server | Product | — | No correctness defect found | Cross-package multi-file publication, reverse discovery, dependency-only evolution, relocatable two-version closure codegen, historical verification, and imported-file deletion guard pass |
| — | RW-07 server | Product | — | No correctness defect found | Private project discovery, Reader/Writer denial, cross-tenant HTTP 403, repository-scoped search, and authorized service codegen pass |

A product finding receives a stable `RW-<scenario>-<number>` identifier and one
of these severities:

- **Release blocker**: data loss/corruption, authorization or isolation breach,
  silent incompatible publication, immutable-byte drift, or duplicate Apply.
- **High**: documented safety behavior fails without a safe operational
  workaround.
- **Medium**: a supported workflow is incorrect or materially confusing but
  fails visibly and preserves data.
- **Low**: diagnostics, documentation, ergonomics, or harness behavior is
  misleading without changing SchemaHub state.

## GA Feedback Loop

For each scenario: run from clean fixtures, triage the ledger, add the smallest
reproduction, fix in scope, rerun the focused case, and then rerun the complete
portfolio. All seven rows have now passed at least once from clean release-mode
state. SchemaHub is scenario-ready for GA only while the complete suite stays
green in CI, every release blocker and high-severity finding is resolved, and
the generated evidence still agrees with the documented 1.0 boundaries. The
machine report enforces those scenario conditions; the stable release workflow
separately enforces the protected staging attestation, while tag authorization
remains an explicit user action.
