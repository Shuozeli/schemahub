openapi: "3.1.0"
info:
  title: "Acme API"
  version: "1.0.0"
paths:
  /users:
    get:
      operationId: listUsers
      summary: List all users
      responses:
        '200':
          description: Success
components:
  schemas:
    User:
      type: object
      description: A user object
      properties:
        id:
          type: string
        name:
          type: string
